# build output
dist/
.output/

# dependencies
node_modules/

# astro
.astro/

# logs
*.log
npm-debug.log*

# environment
.env
.env.production

# macOS
.DS_Store

# working assets — personal reference photos + large source PSDs/PNGs.
# The site only needs the optimized files in public/hero/, which ARE committed.
# Anchored to the repo root so it does NOT catch src/assets/ (Astro-optimized
# source images that MUST be tracked for the build).
/assets/

# local tooling / agent skills / editor + session config
.agents/
.claude/
skills-lock.json

# large work-section asset scratch (AI gens + auto-trace intermediates) —
# the site only uses the optimized files committed in public/work-assets/.
public/work-assets/raw/
public/work-assets/scene-src/
.DS_Store
